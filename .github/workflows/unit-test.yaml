name: UnitTest
on:
  workflow_call:

jobs:
  unit-test:
    name: UnitTest
    runs-on:
      - self-hosted
      - k8s-api
      - linux-amd64-2-4096
      - burstable
      - linux-amd64-s

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: UnitTest
        id: build
        run: |
          echo "This is a UnitTest stage"
